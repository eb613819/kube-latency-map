{
  "name": "kube-latency-map",
  "version": "1.0.0",
  "description": "HTTP round-trip latency visualizer for Kubernetes pods",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@kubernetes/client-node": "^0.20.0"
  }
}